# import base64
# import secrets

# # Generate a random secret key and encode it as base32
# secret_key = secrets.token_bytes(20)  # Generate 20 random bytes
# base32_secret_key = base64.b32encode(secret_key).decode()
# print(base32_secret_key)
